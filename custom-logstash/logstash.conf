input {
    beats {
        port => 5044
    }
}

filter {
    date {
        match => [ "timestamp", "dd/MMM/yyyy HH:mm:ss", "dd/MMM/yyyy:HH:mm:ss Z", "ISO8601" ]
        target => "@timestamp"
    }
}

output {
    elasticsearch {
        hosts => ["http://elasticsearch:9200"]
        index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
        document_type => "%{[@metadata][type]}"
        manage_template => false
    }
}